# Exercise 05 — Sources

## Exercise 1
What file do you declare sources in and where does that file live in your project?

## Exercise 2
Write the `source()` call to reference the `web_sales` table from the `tpcds` source:

```sql
-- your answer here
```

## Exercise 3
What would happen if you used `source('tpcds', 'date_dim')` in a model but forgot to declare `date_dim` in `sources.yml`?

## Exercise 4
You are moving your raw data from `SNOWFLAKE_SAMPLE_DATA` to a new database called `RAW_DATA`. What is the only file you need to update and what line do you change?

## Exercise 5
What is the difference between `ref()` and `source()`? When do you use each?
